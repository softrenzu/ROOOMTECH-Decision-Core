name: performance-smoke

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/performance-smoke.yml"
      - "app/fastpath.py"
      - "app/performance.py"
      - "app/realtime.py"
      - "benchmarks/http_realtime_load.py"
      - "benchmarks/websocket_realtime_load.py"

jobs:
  measure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip
      - run: pip install -e '.[dev]'
      - name: Start API
        run: |
          uvicorn app.main:app --host 127.0.0.1 --port 8000 > /tmp/rtdc-uvicorn.log 2>&1 &
          echo $! > /tmp/rtdc-uvicorn.pid
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:8000/health >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/rtdc-uvicorn.log
          exit 1
      - name: Create rules fast profile
        run: |
          curl -fsS -X POST http://127.0.0.1:8000/v1/realtime/profiles \
            -H 'content-type: application/json' \
            -d '{"profile_id":"perf-route","kind":"route","target_ms":150,"request":{"provider":"rules","min_confidence":0.5,"routes":[{"id":"billing","keywords":["請求","invoice"]},{"id":"account","keywords":["ログイン","password"]}]}}' \
            | tee fast-profile.json
      - name: Measure HTTP realtime fast path
        run: |
          python benchmarks/http_realtime_load.py \
            --base-url http://127.0.0.1:8000 \
            --profile-id perf-route \
            --input 'ログインできません。パスワードを変更したいです' \
            --requests 5000 \
            --concurrency 64 \
            | tee realtime-http.json
      - name: Measure WebSocket realtime fast path
        run: |
          python benchmarks/websocket_realtime_load.py \
            --ws-url ws://127.0.0.1:8000/v1/realtime/ws \
            --profile-id perf-route \
            --input 'ログインできません。パスワードを変更したいです' \
            --requests 5000 \
            --connections 32 \
            | tee realtime-ws.json
      - name: Measure 10k-item MapReduce
        run: |
          python - <<'PY' | tee mapreduce-load.json
          import json, httpx
          items = [{"text": "返金をお願いします" if i % 4 == 0 else "配送状況を確認したいです"} for i in range(10000)]
          payload = {
              "request": {
                  "items": items,
                  "map": {
                      "kind": "detect",
                      "input_field": "text",
                      "config": {
                          "property": "refund request",
                          "provider": "rules",
                          "threshold": 0.8,
                          "keywords": ["返金"]
                      }
                  },
                  "reduce": {"kind": "count_by", "path": "detected"},
                  "concurrency": 64,
                  "include_map_results": False
              },
              "repeat_runs": 3,
              "warmup_runs": 0
          }
          response = httpx.post("http://127.0.0.1:8000/v1/benchmarks/mapreduce-load", json=payload, timeout=120.0)
          response.raise_for_status()
          print(json.dumps(response.json(), ensure_ascii=False, indent=2))
          PY
      - name: Enforce regression gates
        run: |
          python - <<'PY'
          import json
          http = json.load(open("realtime-http.json"))
          ws = json.load(open("realtime-ws.json"))
          mr = json.load(open("mapreduce-load.json"))
          assert http["errors"] == 0, http
          assert http["server_target_hit_rate"] >= 0.999, http
          assert http["roundtrip_p95_ms"] < 250, http
          assert ws["errors"] == 0, ws
          assert ws["server_target_hit_rate"] >= 0.999, ws
          assert ws["roundtrip_p95_ms"] < 150, ws
          assert ws["server_p95_ms"] < 150, ws
          assert mr["total_failed"] == 0, mr
          assert mr["latency"]["throughput_per_second"] > 10000, mr
          print("performance gates passed")
          PY
      - name: Upload measured results
        uses: actions/upload-artifact@v4
        with:
          name: performance-smoke
          path: |
            fast-profile.json
            realtime-http.json
            realtime-ws.json
            mapreduce-load.json
