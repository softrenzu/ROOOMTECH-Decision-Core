services:
  decision-core:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data/models:/app/data/models
    gpus: all
    restart: unless-stopped
